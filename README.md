What is the purpose of this program(s)? finds the max, median, mean, and mode for “max_pos_gs” and “max_neg_gs” and prints these values as a tuple to a new CSV file
What does the program do, include what it takes for input, and what it gives as output? The input would be the data from my rollercoasters.csv, specifically “max_pos_gs” and “max_neg_gs”. and the output is the new csv file
How do you use the program? you run it and it creates a csv called gs_statistics
